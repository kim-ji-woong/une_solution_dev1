<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sopComponentComponentMapper">

	<select id="selectSopComponentComponents" parameterType="map" resultType="egovframework.base.model.sop.component.SopComponentComponent">
		Select * from so_compn
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertSopComponentComponent" parameterType="egovframework.base.model.sop.component.SopComponentComponent" useGeneratedKeys="true" keyProperty="compn_sn">
		INSERT INTO so_compn (grid_sn, column_no, row_no, compn_optn_code, compn_code, step_memb_sn)
		VALUES (#{grid_sn}, #{column_no}, #{row_no}, #{compn_optn_code}, #{compn_code}, #{step_memb_sn})
	</insert>

	<insert id="insertSopComponentComponents" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="compn_sn">
		INSERT INTO so_compn (grid_sn, column_no, row_no, compn_optn_code, compn_code, step_memb_sn)
		VALUES
		<foreach collection="list" item="sopComponentComponent" separator=",">
			(
				(#{sopComponentComponent.grid_sn}, #{sopComponentComponent.column_no}, #{sopComponentComponent.row_no}, #{sopComponentComponent.compn_optn_code}, #{sopComponentComponent.compn_code}, #{sopComponentComponent.step_memb_sn})
			)
		</foreach>
	</insert>

	<update id="updateSopComponentComponent" parameterType="egovframework.base.model.sop.component.SopComponentComponent">
		UPDATE so_compn
		SET grid_sn = #{grid_sn}, column_no = #{column_no}, row_no = #{row_no}, compn_optn_code = #{compn_optn_code}, compn_code = #{compn_code}, step_memb_sn = #{step_memb_sn}
		<where>
			compn_sn = #{compn_sn}
		</where>
	</update>

	<update id="updateSopComponentComponentByCondition" parameterType="map">
		UPDATE so_compn
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteSopComponentComponent" parameterType="egovframework.base.model.sop.component.SopComponentComponent">
		DELETE FROM so_compn
		<where>
			compn_sn = #{compn_sn}
		</where>
	</delete>

	<delete id="deleteSopComponentComponentByCondition" parameterType="map">
		DELETE FROM so_compn
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
