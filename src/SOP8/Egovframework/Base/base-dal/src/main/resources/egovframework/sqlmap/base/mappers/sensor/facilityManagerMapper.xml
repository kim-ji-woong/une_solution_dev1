<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="facilityManagerMapper">

	<select id="selectFacilityManagers" parameterType="map" resultType="egovframework.base.model.sensor.FacilityManager">
		Select * from fa_sensor_fclty_mgr
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertFacilityManager" parameterType="egovframework.base.model.sensor.FacilityManager">
		INSERT INTO fa_sensor_fclty_mgr (fclty_mgr_sn, sensor_ty_optn_code, sensor_ty_code, detct_ty_optn_code, detct_ty_code, site_sn, buld_group_sn, buld_sn, eqp_zone_sn)
		VALUES (#{fclty_mgr_sn}, #{sensor_ty_optn_code}, #{sensor_ty_code}, #{detct_ty_optn_code}, #{detct_ty_code}, #{site_sn}, #{buld_group_sn}, #{buld_sn}, #{eqp_zone_sn})
	</insert>

	<insert id="insertFacilityManagers" parameterType="java.util.List">
		INSERT INTO fa_sensor_fclty_mgr (fclty_mgr_sn, sensor_ty_optn_code, sensor_ty_code, detct_ty_optn_code, detct_ty_code, site_sn, buld_group_sn, buld_sn, eqp_zone_sn)
		VALUES
		<foreach collection="list" item="facilityManager" separator=",">
			(
				(#{facilityManager.fclty_mgr_sn}, #{facilityManager.sensor_ty_optn_code}, #{facilityManager.sensor_ty_code}, #{facilityManager.detct_ty_optn_code}, #{facilityManager.detct_ty_code}, #{facilityManager.site_sn}, #{facilityManager.buld_group_sn}, #{facilityManager.buld_sn}, #{facilityManager.eqp_zone_sn})
			)
		</foreach>
	</insert>

	<update id="updateFacilityManager" parameterType="egovframework.base.model.sensor.FacilityManager">
		UPDATE fa_sensor_fclty_mgr
		SET sensor_ty_optn_code = #{sensor_ty_optn_code}, sensor_ty_code = #{sensor_ty_code}, detct_ty_optn_code = #{detct_ty_optn_code}, detct_ty_code = #{detct_ty_code}, site_sn = #{site_sn}, buld_group_sn = #{buld_group_sn}, buld_sn = #{buld_sn}, eqp_zone_sn = #{eqp_zone_sn}
		<where>
			fclty_mgr_sn = #{fclty_mgr_sn}
		</where>
	</update>

	<update id="updateFacilityManagerByCondition" parameterType="map">
		UPDATE fa_sensor_fclty_mgr
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteFacilityManager" parameterType="egovframework.base.model.sensor.FacilityManager">
		DELETE FROM fa_sensor_fclty_mgr
		<where>
			fclty_mgr_sn = #{fclty_mgr_sn}
		</where>
	</delete>

	<delete id="deleteFacilityManagerByCondition" parameterType="map">
		DELETE FROM fa_sensor_fclty_mgr
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
