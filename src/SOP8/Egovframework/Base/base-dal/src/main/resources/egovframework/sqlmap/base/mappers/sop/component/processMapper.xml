<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="processMapper">

	<select id="selectProcesses" parameterType="map" resultType="egovframework.base.model.sop.component.Process">
		Select * from so_compn_procs
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertProcess" parameterType="egovframework.base.model.sop.component.Process">
		INSERT INTO so_compn_procs (compn_sn, title, leadr_prvuse_yn, atmc_execut_yn, execut_no)
		VALUES (#{compn_sn}, #{title}, #{leadr_prvuse_yn}, #{atmc_execut_yn}, #{execut_no})
	</insert>

	<insert id="insertProcesses" parameterType="java.util.List">
		INSERT INTO so_compn_procs (compn_sn, title, leadr_prvuse_yn, atmc_execut_yn, execut_no)
		VALUES
		<foreach collection="list" item="process" separator=",">
			(
				(#{process.compn_sn}, #{process.title}, #{process.leadr_prvuse_yn}, #{process.atmc_execut_yn}, #{process.execut_no})
			)
		</foreach>
	</insert>

	<update id="updateProcess" parameterType="egovframework.base.model.sop.component.Process">
		UPDATE so_compn_procs
		SET title = #{title}, leadr_prvuse_yn = #{leadr_prvuse_yn}, atmc_execut_yn = #{atmc_execut_yn}, execut_no = #{execut_no}
		<where>
			compn_sn = #{compn_sn}
		</where>
	</update>

	<update id="updateProcessByCondition" parameterType="map">
		UPDATE so_compn_procs
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteProcess" parameterType="egovframework.base.model.sop.component.Process">
		DELETE FROM so_compn_procs
		<where>
			compn_sn = #{compn_sn}
		</where>
	</delete>

	<delete id="deleteProcessByCondition" parameterType="map">
		DELETE FROM so_compn_procs
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
