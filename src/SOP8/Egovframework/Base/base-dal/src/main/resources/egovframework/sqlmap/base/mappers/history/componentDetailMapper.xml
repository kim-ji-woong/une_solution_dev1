<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="componentDetailMapper">

	<select id="selectComponentDetails" parameterType="map" resultType="egovframework.base.model.history.ComponentDetail">
		Select * from his_compn_detail
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertComponentDetail" parameterType="egovframework.base.model.history.ComponentDetail" useGeneratedKeys="true" keyProperty="compn_hist_detail_sn">
		INSERT INTO his_compn_detail (compn_hist_sn, data_no, data_intgr, data_float, data_str)
		VALUES (#{compn_hist_sn}, #{data_no}, #{data_intgr}, #{data_float}, #{data_str})
	</insert>

	<insert id="insertComponentDetails" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="compn_hist_detail_sn">
		INSERT INTO his_compn_detail (compn_hist_sn, data_no, data_intgr, data_float, data_str)
		VALUES
		<foreach collection="list" item="componentDetail" separator=",">
			(
				(#{componentDetail.compn_hist_sn}, #{componentDetail.data_no}, #{componentDetail.data_intgr}, #{componentDetail.data_float}, #{componentDetail.data_str})
			)
		</foreach>
	</insert>

	<update id="updateComponentDetail" parameterType="egovframework.base.model.history.ComponentDetail">
		UPDATE his_compn_detail
		SET compn_hist_sn = #{compn_hist_sn}, data_no = #{data_no}, data_intgr = #{data_intgr}, data_float = #{data_float}, data_str = #{data_str}
		<where>
			compn_hist_detail_sn = #{compn_hist_detail_sn}
		</where>
	</update>

	<update id="updateComponentDetailByCondition" parameterType="map">
		UPDATE his_compn_detail
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteComponentDetail" parameterType="egovframework.base.model.history.ComponentDetail">
		DELETE FROM his_compn_detail
		<where>
			compn_hist_detail_sn = #{compn_hist_detail_sn}
		</where>
	</delete>

	<delete id="deleteComponentDetailByCondition" parameterType="map">
		DELETE FROM his_compn_detail
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
