<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sensorZoneMapper">

	<select id="selectSensorZones" parameterType="map" resultType="egovframework.base.model.history.SensorZone">
		Select * from his_sensor_zone
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertSensorZone" parameterType="egovframework.base.model.history.SensorZone" useGeneratedKeys="true" keyProperty="sensor_zone_hist_sn">
		INSERT INTO his_sensor_zone (tm, zone_sn, sensor_ty_optn_code, sensor_ty_code, detct_sttus_optn_code, detct_sttus_code, memo, site_sn, reportr)
		VALUES (#{tm}, #{zone_sn}, #{sensor_ty_optn_code}, #{sensor_ty_code}, #{detct_sttus_optn_code}, #{detct_sttus_code}, #{memo}, #{site_sn}, #{reportr})
	</insert>

	<insert id="insertSensorZones" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="sensor_zone_hist_sn">
		INSERT INTO his_sensor_zone (tm, zone_sn, sensor_ty_optn_code, sensor_ty_code, detct_sttus_optn_code, detct_sttus_code, memo, site_sn, reportr)
		VALUES
		<foreach collection="list" item="sensorZone" separator=",">
			(
				(#{sensorZone.tm}, #{sensorZone.zone_sn}, #{sensorZone.sensor_ty_optn_code}, #{sensorZone.sensor_ty_code}, #{sensorZone.detct_sttus_optn_code}, #{sensorZone.detct_sttus_code}, #{sensorZone.memo}, #{sensorZone.site_sn}, #{sensorZone.reportr})
			)
		</foreach>
	</insert>

	<update id="updateSensorZone" parameterType="egovframework.base.model.history.SensorZone">
		UPDATE his_sensor_zone
		SET tm = #{tm}, zone_sn = #{zone_sn}, sensor_ty_optn_code = #{sensor_ty_optn_code}, sensor_ty_code = #{sensor_ty_code}, detct_sttus_optn_code = #{detct_sttus_optn_code}, detct_sttus_code = #{detct_sttus_code}, memo = #{memo}, site_sn = #{site_sn}, reportr = #{reportr}
		<where>
			sensor_zone_hist_sn = #{sensor_zone_hist_sn}
		</where>
	</update>

	<update id="updateSensorZoneByCondition" parameterType="map">
		UPDATE his_sensor_zone
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteSensorZone" parameterType="egovframework.base.model.history.SensorZone">
		DELETE FROM his_sensor_zone
		<where>
			sensor_zone_hist_sn = #{sensor_zone_hist_sn}
		</where>
	</delete>

	<delete id="deleteSensorZoneByCondition" parameterType="map">
		DELETE FROM his_sensor_zone
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
