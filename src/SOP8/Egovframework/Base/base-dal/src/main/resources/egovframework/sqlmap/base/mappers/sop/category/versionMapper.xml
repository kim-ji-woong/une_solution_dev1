<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="versionMapper">

	<select id="selectVersions" parameterType="map" resultType="egovframework.base.model.sop.category.Version">
		Select * from so_ctgry_ver
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertVersion" parameterType="egovframework.base.model.sop.category.Version" useGeneratedKeys="true" keyProperty="ver_sn">
		INSERT INTO so_ctgry_ver (creat_de, last_acces_de, name, user_sn, site_sn, descp)
		VALUES (#{creat_de}, #{last_acces_de}, #{name}, #{user_sn}, #{site_sn}, #{descp})
	</insert>

	<insert id="insertVersions" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="ver_sn">
		INSERT INTO so_ctgry_ver (creat_de, last_acces_de, name, user_sn, site_sn, descp)
		VALUES
		<foreach collection="list" item="version" separator=",">
			(
				(#{version.creat_de}, #{version.last_acces_de}, #{version.name}, #{version.user_sn}, #{version.site_sn}, #{version.descp})
			)
		</foreach>
	</insert>

	<update id="updateVersion" parameterType="egovframework.base.model.sop.category.Version">
		UPDATE so_ctgry_ver
		SET creat_de = #{creat_de}, last_acces_de = #{last_acces_de}, name = #{name}, user_sn = #{user_sn}, site_sn = #{site_sn}, descp = #{descp}
		<where>
			ver_sn = #{ver_sn}
		</where>
	</update>

	<update id="updateVersionByCondition" parameterType="map">
		UPDATE so_ctgry_ver
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteVersion" parameterType="egovframework.base.model.sop.category.Version">
		DELETE FROM so_ctgry_ver
		<where>
			ver_sn = #{ver_sn}
		</where>
	</delete>

	<delete id="deleteVersionByCondition" parameterType="map">
		DELETE FROM so_ctgry_ver
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
