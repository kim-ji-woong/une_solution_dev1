<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="materialMapper">

	<select id="selectMaterials" parameterType="map" resultType="egovframework.base.model.sensor.Material">
		Select * from fa_sensor_material
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertMaterial" parameterType="egovframework.base.model.sensor.Material">
		INSERT INTO fa_sensor_material (sensor_zone_sn, cur_data, lim_bas, sensor_lim_ty_optn_code, sensor_lim_ty)
		VALUES (#{sensor_zone_sn}, #{cur_data}, #{lim_bas}, #{sensor_lim_ty_optn_code}, #{sensor_lim_ty})
	</insert>

	<insert id="insertMaterials" parameterType="java.util.List">
		INSERT INTO fa_sensor_material (sensor_zone_sn, cur_data, lim_bas, sensor_lim_ty_optn_code, sensor_lim_ty)
		VALUES
		<foreach collection="list" item="material" separator=",">
			(
				(#{material.sensor_zone_sn}, #{material.cur_data}, #{material.lim_bas}, #{material.sensor_lim_ty_optn_code}, #{material.sensor_lim_ty})
			)
		</foreach>
	</insert>

	<update id="updateMaterial" parameterType="egovframework.base.model.sensor.Material">
		UPDATE fa_sensor_material
		SET cur_data = #{cur_data}, lim_bas = #{lim_bas}, sensor_lim_ty_optn_code = #{sensor_lim_ty_optn_code}, sensor_lim_ty = #{sensor_lim_ty}
		<where>
			sensor_zone_sn = #{sensor_zone_sn}
		</where>
	</update>

	<update id="updateMaterialByCondition" parameterType="map">
		UPDATE fa_sensor_material
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteMaterial" parameterType="egovframework.base.model.sensor.Material">
		DELETE FROM fa_sensor_material
		<where>
			sensor_zone_sn = #{sensor_zone_sn}
		</where>
	</delete>

	<delete id="deleteMaterialByCondition" parameterType="map">
		DELETE FROM fa_sensor_material
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
