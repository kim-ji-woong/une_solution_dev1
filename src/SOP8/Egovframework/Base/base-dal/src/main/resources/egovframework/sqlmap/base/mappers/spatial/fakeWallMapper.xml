<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="fakeWallMapper">

	<select id="selectFakeWalls" parameterType="map" resultType="egovframework.base.model.spatial.FakeWall">
		Select * from sp_fake_wall
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertFakeWall" parameterType="egovframework.base.model.spatial.FakeWall">
		INSERT INTO sp_fake_wall (fake_wall_sn, zone_sn, x, y, z, rtate, scale)
		VALUES (#{fake_wall_sn}, #{zone_sn}, #{x}, #{y}, #{z}, #{rtate}, #{scale})
	</insert>

	<insert id="insertFakeWalls" parameterType="java.util.List">
		INSERT INTO sp_fake_wall (fake_wall_sn, zone_sn, x, y, z, rtate, scale)
		VALUES
		<foreach collection="list" item="fakeWall" separator=",">
			(
				(#{fakeWall.fake_wall_sn}, #{fakeWall.zone_sn}, #{fakeWall.x}, #{fakeWall.y}, #{fakeWall.z}, #{fakeWall.rtate}, #{fakeWall.scale})
			)
		</foreach>
	</insert>

	<update id="updateFakeWall" parameterType="egovframework.base.model.spatial.FakeWall">
		UPDATE sp_fake_wall
		SET zone_sn = #{zone_sn}, x = #{x}, y = #{y}, z = #{z}, rtate = #{rtate}, scale = #{scale}
		<where>
			fake_wall_sn = #{fake_wall_sn}
		</where>
	</update>

	<update id="updateFakeWallByCondition" parameterType="map">
		UPDATE sp_fake_wall
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteFakeWall" parameterType="egovframework.base.model.spatial.FakeWall">
		DELETE FROM sp_fake_wall
		<where>
			fake_wall_sn = #{fake_wall_sn}
		</where>
	</delete>

	<delete id="deleteFakeWallByCondition" parameterType="map">
		DELETE FROM sp_fake_wall
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
