<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="serverInfoMapper">

	<select id="selectServerInfos" parameterType="map" resultType="egovframework.base.model.sensor.ServerInfo">
		Select * from fa_sensor_server
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertServerInfo" parameterType="egovframework.base.model.sensor.ServerInfo">
		INSERT INTO fa_sensor_server (sensor_server_sn, lc, ip, port, cnnc_sttus, alarm_server_url, usab, sensor_server_ty_optn_code, sensor_server_ty_code, site_sn)
		VALUES (#{sensor_server_sn}, #{lc}, #{ip}, #{port}, #{cnnc_sttus}, #{alarm_server_url}, #{usab}, #{sensor_server_ty_optn_code}, #{sensor_server_ty_code}, #{site_sn})
	</insert>

	<insert id="insertServerInfos" parameterType="java.util.List">
		INSERT INTO fa_sensor_server (sensor_server_sn, lc, ip, port, cnnc_sttus, alarm_server_url, usab, sensor_server_ty_optn_code, sensor_server_ty_code, site_sn)
		VALUES
		<foreach collection="list" item="serverInfo" separator=",">
			(
				(#{serverInfo.sensor_server_sn}, #{serverInfo.lc}, #{serverInfo.ip}, #{serverInfo.port}, #{serverInfo.cnnc_sttus}, #{serverInfo.alarm_server_url}, #{serverInfo.usab}, #{serverInfo.sensor_server_ty_optn_code}, #{serverInfo.sensor_server_ty_code}, #{serverInfo.site_sn})
			)
		</foreach>
	</insert>

	<update id="updateServerInfo" parameterType="egovframework.base.model.sensor.ServerInfo">
		UPDATE fa_sensor_server
		SET lc = #{lc}, ip = #{ip}, port = #{port}, cnnc_sttus = #{cnnc_sttus}, alarm_server_url = #{alarm_server_url}, usab = #{usab}, sensor_server_ty_optn_code = #{sensor_server_ty_optn_code}, sensor_server_ty_code = #{sensor_server_ty_code}, site_sn = #{site_sn}
		<where>
			sensor_server_sn = #{sensor_server_sn}
		</where>
	</update>

	<update id="updateServerInfoByCondition" parameterType="map">
		UPDATE fa_sensor_server
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteServerInfo" parameterType="egovframework.base.model.sensor.ServerInfo">
		DELETE FROM fa_sensor_server
		<where>
			sensor_server_sn = #{sensor_server_sn}
		</where>
	</delete>

	<delete id="deleteServerInfoByCondition" parameterType="map">
		DELETE FROM fa_sensor_server
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
