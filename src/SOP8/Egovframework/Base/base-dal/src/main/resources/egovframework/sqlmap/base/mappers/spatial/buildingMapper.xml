<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC " -//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="buildingMapper">

	<select id="selectBuildings" parameterType="map" resultType="egovframework.base.model.spatial.Building">
		Select * from sp_buld
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
		<if test="orderBy != null and orderBy != ''">
		ORDER BY ${orderBy}
		</if>
	</select>

	<insert id="insertBuilding" parameterType="egovframework.base.model.spatial.Building">
		INSERT INTO sp_buld (buld_sn, buld_code, name, buld_group_sn, top_floor_indx, min_floor_indx, text_center_crdnt_x, text_center_crdnt_y, text_center_crdnt_z, brdcst_text, disp_text)
		VALUES (#{buld_sn}, #{buld_code}, #{name}, #{buld_group_sn}, #{top_floor_indx}, #{min_floor_indx}, #{text_center_crdnt_x}, #{text_center_crdnt_y}, #{text_center_crdnt_z}, #{brdcst_text}, #{disp_text})
	</insert>

	<insert id="insertBuildings" parameterType="java.util.List">
		INSERT INTO sp_buld (buld_sn, buld_code, name, buld_group_sn, top_floor_indx, min_floor_indx, text_center_crdnt_x, text_center_crdnt_y, text_center_crdnt_z, brdcst_text, disp_text)
		VALUES
		<foreach collection="list" item="building" separator=",">
			(
				(#{building.buld_sn}, #{building.buld_code}, #{building.name}, #{building.buld_group_sn}, #{building.top_floor_indx}, #{building.min_floor_indx}, #{building.text_center_crdnt_x}, #{building.text_center_crdnt_y}, #{building.text_center_crdnt_z}, #{building.brdcst_text}, #{building.disp_text})
			)
		</foreach>
	</insert>

	<update id="updateBuilding" parameterType="egovframework.base.model.spatial.Building">
		UPDATE sp_buld
		SET buld_code = #{buld_code}, name = #{name}, buld_group_sn = #{buld_group_sn}, top_floor_indx = #{top_floor_indx}, min_floor_indx = #{min_floor_indx}, text_center_crdnt_x = #{text_center_crdnt_x}, text_center_crdnt_y = #{text_center_crdnt_y}, text_center_crdnt_z = #{text_center_crdnt_z}, brdcst_text = #{brdcst_text}, disp_text = #{disp_text}
		<where>
			buld_sn = #{buld_sn}
		</where>
	</update>

	<update id="updateBuildingByCondition" parameterType="map">
		UPDATE sp_buld
		SET ${setStatement}
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</update>

	<delete id="deleteBuilding" parameterType="egovframework.base.model.spatial.Building">
		DELETE FROM sp_buld
		<where>
			buld_sn = #{buld_sn}
		</where>
	</delete>

	<delete id="deleteBuildingByCondition" parameterType="map">
		DELETE FROM sp_buld
		<if test="customCondition != null and customCondition != ''">
			WHERE ${customCondition}
		</if>
	</delete>

</mapper>
