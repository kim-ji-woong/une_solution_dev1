using Base.Model.Sensor;
using dnsDapperDBUtil.DataAccessLayer.IDAL;

namespace Base.SDMS.BLL.Process
{
    public class ManualAlarmChecker
    {
        /*public static bool IsManualAlarm(SensorZone sensorZone, IDataManager dataManager)
        {
            if (sensorZone.sensor_sub_ty_no == null && sensorZone.sensor_server_sn == null)
                return true;

            return false;
        }*/
    }
}
